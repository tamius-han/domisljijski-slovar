// This file contains all the .env fields the
// project uses and serves purely as a non-working
// sample.

const env = {
  GOOGLE_CLIENT_ID: 'id',
  DICTIONARY_API_BASE: 'url',
  ADMIN_API_BASE: 'url'
};

export default env;