
function legacy2word(wordsEn, wordsSl, translations) {

  const newWords = [];
  const newMeanings = [];

  for (const word of wordsEn) {
    if (['dragon', 'dragonborn', 'orc', 'ork', 'goblin', 'direwolf'].includes(word)) {

    }
  }


}